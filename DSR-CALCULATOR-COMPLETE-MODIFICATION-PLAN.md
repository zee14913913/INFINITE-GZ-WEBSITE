# 🔧 DSR计算器完整修改计划（整合版）

**创建日期**: 2025-01-29
**最后更新**: 2025-01-29
**目标文件**: `components/DSRCalculator.tsx` 和 `lib/bankStandardsReal2025.ts`
**优先级**: 🔴 高优先级改进

---

## 📋 目录

1. [修改概览](#修改概览)
2. [核心修改项（高优先级）](#核心修改项高优先级)
3. [功能增强（中优先级）](#功能增强中优先级)
4. [新银行数据收集与整合](#新银行数据收集与整合)
5. [SME Loan（企业贷款）类型添加](#sme-loan企业贷款类型添加)
6. [实施建议与时间表](#实施建议与时间表)

---

## 📋 修改概览

| 序号 | 修改项 | 优先级 | 预计工作量 | 影响范围 |
|------|--------|--------|-----------|---------|
| 1 | 修复月供计算公式 | 🔴 高 | 30分钟 | 计算结果准确性 |
| 2 | 使用完整银行标准数据 | 🔴 高 | 1小时 | 银行对比准确性 |
| 3 | 应用身份类型规则 | 🔴 高 | 1.5小时 | DSR计算逻辑 |
| 4 | 添加SME Loan贷款类型 | 🔴 高 | 1小时 | 数据结构扩展 |
| 5 | 整合7家新银行数据 | 🔴 高 | 2小时 | 银行覆盖范围 |
| 6 | 增强收入计算 | 🟡 中 | 1小时 | Step 2功能 |
| 7 | 增强债务管理 | 🟡 中 | 1小时 | Step 3功能 |
| 8 | 添加结果导出 | 🟡 中 | 2小时 | Step 4功能 |

**总工作量估算**: 约10小时

---

## 🔴 核心修改项（高优先级）

---

### 修改项1: 修复月供计算公式

#### 问题描述

**当前代码位置**: `components/DSRCalculator.tsx` 第115-120行

**当前实现（简化公式）**:
```javascript
// 计算新贷款月供（平息法简化）
useEffect(() => {
  const monthlyInterest = (loanAmount * interestRate * loanYears) / 100 / 12 / loanYears;
  const monthlyPrincipal = loanAmount / (loanYears * 12);
  setNewMonthlyPayment(monthlyPrincipal + monthlyInterest);
}, [loanAmount, loanYears, interestRate]);
```

**问题**:
- 使用简化的平息法公式，**计算结果不准确**
- 不符合马来西亚银行的真实计算方式
- 会导致月供金额偏高，影响DSR计算准确性

**正确公式（减余法）**:
```
月供 = 本金 × [r(1+r)^n] / [(1+r)^n - 1]

其中：
r = 月利率 = 年利率 / 12
n = 总月数 = 年数 × 12
```

#### 修改方案

**方案B: 使用库函数（推荐）**

**Step 1**: 在文件顶部添加导入
```javascript
import { calculateDiminishingRateMonthlyPayment } from '@/lib/dsrCalculator';
```

**Step 2**: 修改useEffect（第115-120行）
```javascript
// 计算新贷款月供（使用库函数 - 减余法）
useEffect(() => {
  if (loanAmount > 0 && interestRate > 0 && loanYears > 0) {
    const result = calculateDiminishingRateMonthlyPayment(
      loanAmount,
      loanYears,
      interestRate / 100 // 转换为小数（如7% = 0.07）
    );
    setNewMonthlyPayment(result.monthlyPayment);
  } else {
    setNewMonthlyPayment(0);
  }
}, [loanAmount, loanYears, interestRate]);
```

#### 验证测试

**测试用例**:
```javascript
// 测试用例1: 标准贷款
贷款金额: RM 100,000
利率: 7% p.a.
期限: 5年
预期月供: 约 RM 1,980.12

// 测试用例2: 房贷
贷款金额: RM 500,000
利率: 4.5% p.a.
期限: 30年
预期月供: 约 RM 2,533.43
```

---

### 修改项2: 使用完整银行标准数据

#### 问题描述

**当前代码位置**: `components/DSRCalculator.tsx` 第45-55行（硬编码数据）

**问题**:
- 数据过于简化，只有DSR限制和自雇比例
- 缺少不同贷款类型的DSR限制
- 缺少不同收入区间的DSR限制
- 缺少身份类型特殊条件

**完整数据源**: `lib/bankStandardsReal2025.ts`

#### 修改方案

**Step 1**: 导入完整银行标准数据（文件顶部）
```javascript
import {
  BANK_STANDARDS_REAL_2025,
  getDSRLimitForBank,
  getIncomeRecognitionRate,
  type BankStandardReal
} from '@/lib/bankStandardsReal2025';
```

**Step 2**: 创建辅助函数（组件定义前）
```javascript
// 获取银行的DSR限制（基于收入、贷款类型）
function getBankDSRLimit(
  bankCode: string,
  netIncome: number,
  loanType: string
): number {
  try {
    const bank = BANK_STANDARDS_REAL_2025.find(b => b.bankCode === bankCode);
    if (!bank) return 70; // 默认值

    // 根据贷款类型获取DSR限制
    const loanTypeKey = loanType === 'personal' ? 'personal' :
                       loanType === 'housing' ? 'housing' :
                       loanType === 'auto' ? 'car' :
                       loanType === 'business' ? 'sme_loan' : 'personal';

    // 获取该贷款类型的DSR限制数组
    const dsrLimits = bank.dsr[loanTypeKey] || [];

    // 找到匹配收入区间的DSR限制
    for (const limit of dsrLimits) {
      if (netIncome >= limit.minNetIncome &&
          (!limit.maxNetIncome || netIncome <= limit.maxNetIncome)) {
        return limit.dsrLimit;
      }
    }

    // 如果没有匹配，返回最后一个限制
    return dsrLimits[dsrLimits.length - 1]?.dsrLimit || 70;
  } catch (error) {
    console.error('Error getting DSR limit:', error);
    return 70;
  }
}

// 获取银行的收入认定比例
function getBankIncomeRecognitionRate(
  bankCode: string,
  employmentType: string
): number {
  try {
    const bank = BANK_STANDARDS_REAL_2025.find(b => b.bankCode === bankCode);
    if (!bank) return 1.0;

    const employmentKey = employmentType === 'salaried' ? 'salaried' :
                         employmentType === 'self_employed' ? 'self_employed' :
                         employmentType === 'government' ? 'government' : 'contract';

    const rule = bank.incomeRecognition[employmentKey];
    return rule?.recognitionRate || 1.0;
  } catch (error) {
    console.error('Error getting income recognition rate:', error);
    return 1.0;
  }
}
```

**Step 3**: 修改银行评估逻辑（第131-176行）

详细代码请参考原修改计划文档。

---

### 修改项3: 应用身份类型规则

#### 问题描述

身份类型选择后，没有应用到后续计算中。不同身份类型应该有：
- 不同的DSR限制调整
- 不同的利率溢价
- 不同的LTV限制（房贷）
- 不同的文件要求

#### 修改方案

详细代码请参考原修改计划文档。

---

## 🟡 功能增强（中优先级）

### 修改项4: 增强收入计算

**添加字段**:
- 固定津贴 (Allowances)
- 月均奖金 (Bonus Monthly)
- 其他收入 (Other Income)
- 其他扣除 (Other Deductions)

详细实现请参考原修改计划文档。

### 修改项5: 增强债务管理

**添加功能**:
- "其他债务"动态管理
- 更多债务类型支持

详细实现请参考原修改计划文档。

---

## 🏦 新银行数据收集与整合

### 📋 需要添加的银行列表

1. **SCB** - Standard Chartered Bank (渣打银行)
2. **UOB** - United Overseas Bank (大华银行)
3. **AMB** - AmBank (马来亚银行/大马银行)
4. **MBSB** - Malaysia Building Society Berhad (马来西亚建筑储蓄银行)
5. **OCBC** - Oversea-Chinese Banking Corporation (华侨银行)
6. **ALLIANCE** - Alliance Bank Malaysia Berhad (联昌国际银行)
7. **BANK RAKYAT** - Bank Kerjasama Rakyat Malaysia Berhad (人民银行)

### 🔍 搜索到的公开信息

#### 利率信息（2023-2025年数据）

| 银行 | SBR | BR | BLR | ELR | 备注 |
|------|-----|----|----|----|-----|
| **SCB** | 3.00% | 3.64% | 6.74% | 4.75% | 与HSBC类似 |
| **UOB** | 3.00% | 3.83% | 6.76% | 4.70% | 与OCBC类似 |
| **AMB** | 3.00% | 3.60% | 6.45% | 4.25% | 利率较低 |
| **MBSB** | 3.00% | - | - | SBR+15% | 最高15%浮动利率 |
| **OCBC** | 3.00% | 3.83% | 6.76% | 4.70% | 与UOB相同 |
| **ALLIANCE** | 3.00% | 3.82% | 6.67% | 4.36% | 中等利率 |
| **BANK RAKYAT** | 3.00% | - | - | 3.42%-5.22% | 公共部门更优惠 |

#### 个人贷款信息

**AmBank (AMB)**:
- 个人贷款利率: 4.38% - 19.88% (固定利率)
- 贷款期限: 最长7年

**Alliance Bank (ALLIANCE)**:
- 数字个人贷款平利率: 4.99%起，最高16.68%
- 最低收入要求: RM 3,000
- 贷款期限: 最长7年

**UOB**:
- 个人贷款利率: 3.99%起
- 无需抵押
- 期限: 最长5年

**Bank Rakyat**:
- 个人融资-i利率: 3.42% - 5.22%
- 公共部门扣薪方式: 更优惠利率

**MBSB**:
- 个人融资利率: SBR + 最高15.00% (浮动利率)

### 📊 数据推断原则

由于公开信息中缺少详细的DSR限制、收入认定规则等信息，基于以下原则进行合理推断：

1. **参考现有银行模式**: 根据现有8家银行的数据模式
2. **利率水平推断**: 低利率银行通常更严格，高利率银行更宽松
3. **银行类型推断**:
   - 国际银行（SCB, UOB, OCBC）: 通常较严格，但服务更好
   - 本地银行（AMB, ALLIANCE）: 中等严格程度
   - 特定银行（MBSB, BANK RAKYAT）: 可能有特殊政策
4. **行业标准**: 参考马来西亚银行业的一般标准

### 数据推断表

| 银行 | DSR限制推断 | 自雇收入认定推断 | 身份类型政策推断 | 备注 |
|------|------------|----------------|----------------|------|
| **SCB** | 60-70% | 70-75% | 外国人较严格 | 国际银行，较保守 |
| **UOB** | 60-75% | 75-80% | 外国人可能接受 | 新加坡银行，中等严格 |
| **AMB** | 60-70% | 70-75% | 标准政策 | 本地大型银行 |
| **MBSB** | 65-75% | 70-80% | 较宽松 | 建筑储蓄银行，较宽松 |
| **OCBC** | 60-75% | 75-80% | 外国人可能接受 | 新加坡银行，中等严格 |
| **ALLIANCE** | 60-70% | 70-75% | 标准政策 | 本地银行，中等严格 |
| **BANK RAKYAT** | 65-80% | 75-85% | 较宽松 | 合作银行，对公共部门优惠 |

### 🎯 推荐的完整数据结构（7家新银行）

#### 1. Standard Chartered Bank (SCB)

```typescript
{
  bankName: 'Standard Chartered Bank',
  bankCode: 'SCB',

  dsr: {
    personal: [
      { minNetIncome: 0, maxNetIncome: 3000, dsrLimit: 60, notes: '低收入严格' },
      { minNetIncome: 3000, dsrLimit: 70, notes: '高收入' }
    ],
    housing: [
      { minNetIncome: 0, maxNetIncome: 3000, dsrLimit: 60 },
      { minNetIncome: 3000, dsrLimit: 70 }
    ],
    car: [
      { minNetIncome: 0, maxNetIncome: 3000, dsrLimit: 60 },
      { minNetIncome: 3000, dsrLimit: 70 }
    ],
    credit_card: [
      { minNetIncome: 0, maxNetIncome: 3000, dsrLimit: 60 },
      { minNetIncome: 3000, dsrLimit: 70 }
    ],
    sme_loan: [
      { minNetIncome: 0, maxNetIncome: 5000, dsrLimit: 65, notes: '中小企业较宽松' },
      { minNetIncome: 5000, dsrLimit: 75, notes: '大型企业' }
    ]
  },

  incomeRecognition: {
    salaried: {
      employmentType: 'salaried',
      recognitionRate: 1.0,
      requiredDocuments: ['最近3个月工资单', 'EPF单据', '银行账单'],
      notes: '标准认可100%'
    },
    government: {
      employmentType: 'government',
      recognitionRate: 1.0,
      requiredDocuments: ['工资单', 'EPF单据'],
      notes: '标准认可'
    },
    self_employed: {
      employmentType: 'self_employed',
      recognitionRate: 0.75,
      minBusinessYears: 3,
      requiredDocuments: ['过去2年报税单', '12个月银行流水', 'SSM证书'],
      notes: '取2年平均×75%，中等保守'
    },
    contract: {
      employmentType: 'contract',
      recognitionRate: 0.75,
      requiredDocuments: ['合约书', '最近6个月工资单', '雇主信'],
      notes: '需合约至少12个月有效期'
    }
  },

  identityConditions: [
    {
      identity: 'malaysian_citizen',
      accepted: true
    },
    {
      identity: 'permanent_resident',
      accepted: true,
      additionalDocuments: ['PR证书'],
      specialNotes: '接受，需额外文件'
    },
    {
      identity: 'foreigner',
      accepted: true,
      interestRatePremium: 1.0,
      dsrAdjustment: -10,
      specialNotes: '接受但条件严格，需工作3+年，大公司'
    }
  ],

  loanLimits: {
    personalLoanMax: 200000,
    personalLoanMultiplier: 12,
    housingLTV: {
      firstProperty: 90,
      secondProperty: 80,
      thirdProperty: 70
    },
    carLTV: {
      newCar: 90,
      usedCar: 75
    },
    creditCardLimitMultiplier: 2.5
  },

  requirements: {
    minAge: 21,
    maxAge: 65,
    minEmploymentMonths: 6,
    requiresPayslip: true,
    requiresEPF: true,
    requiresBankStatement: true
  },

  specialFeatures: [
    '国际银行，服务较好',
    '对自雇中等保守（75%）',
    '接受外国人但条件严格',
    'SME贷款较宽松（65-75% DSR）'
  ]
}
```

#### 2-7. 其他6家银行数据

（为节省篇幅，其他5家银行的数据结构类似，包含所有贷款类型包括SME Loan。完整代码请参考 `NEW-BANKS-DATA-COLLECTION.md` 文件）

**关键点**: 所有新银行的数据结构都需要包含 `sme_loan` 贷款类型的DSR限制。

---

## 📈 SME Loan（企业贷款）类型添加

### 需求说明

在现有的4种贷款类型基础上，添加第5种贷款类型：**SME Loan（企业贷款）**

**现有贷款类型**:
- `personal` - 个人贷款
- `housing` - 房屋贷款
- `car` - 汽车贷款
- `credit_card` - 信用卡

**新增贷款类型**:
- `sme_loan` - 企业贷款（SME Loan / Business Loan）

### 修改方案

#### Step 1: 更新类型定义

**文件**: `lib/bankStandardsReal2025.ts`

**修改位置**: 第35-39行

**当前代码**:
```typescript
export type LoanType =
  | 'personal'               // 个人贷款
  | 'housing'                // 房贷
  | 'car'                    // 车贷
  | 'credit_card';           // 信用卡
```

**新代码**:
```typescript
export type LoanType =
  | 'personal'               // 个人贷款
  | 'housing'                // 房贷
  | 'car'                    // 车贷
  | 'credit_card'            // 信用卡
  | 'sme_loan';              // 企业贷款（SME Loan / Business Loan）
```

#### Step 2: 更新所有现有银行数据

**需要在所有15家银行（现有8家 + 新增7家）的数据中添加 `sme_loan` DSR限制**

**示例格式**（以Maybank为例）:

在 `dsr` 对象中添加：
```typescript
sme_loan: [
  { minNetIncome: 0, maxNetIncome: 5000, dsrLimit: 65, notes: '中小企业较宽松' },
  { minNetIncome: 5000, dsrLimit: 75, notes: '大型企业' }
]
```

**各银行SME Loan DSR限制建议**:

| 银行 | 低收入区间 (< RM 5,000) | 高收入区间 (≥ RM 5,000) | 备注 |
|------|----------------------|---------------------|------|
| **MBB** | 65% | 75% | 中小企业友好 |
| **CIMB** | 70% | 80% | 对SME较宽松 |
| **RHB** | 60% | 65% | 较严格 |
| **HLB** | 70% | 80% | 较宽松 |
| **PBB** | 65% | 75% | 标准 |
| **HSBC** | 65% | 75% | 标准 |
| **BSN** | 65% | 75% | 标准 |
| **BIMB** | 60% | 70% | 较严格 |
| **SCB** | 65% | 75% | 标准 |
| **UOB** | 70% | 80% | 较宽松 |
| **AMB** | 65% | 75% | 标准 |
| **MBSB** | 70% | 80% | 较宽松 |
| **OCBC** | 70% | 80% | 较宽松 |
| **ALLIANCE** | 65% | 75% | 标准 |
| **BANK RAKYAT** | 70% | 85% | 最宽松（合作银行优势） |

#### Step 3: 更新DSRCalculator组件

**文件**: `components/DSRCalculator.tsx`

**修改位置**: 第37-43行（贷款类型选项）

**当前代码**:
```typescript
const LOAN_TYPES = [
  { value: 'personal', label: 'Personal Loan', labelZh: '个人贷款', labelMs: 'Pinjaman Peribadi' },
  { value: 'housing', label: 'Housing Loan', labelZh: '房屋贷款', labelMs: 'Pinjaman Perumahan' },
  { value: 'auto', label: 'Auto Loan', labelZh: '汽车贷款', labelMs: 'Pinjaman Kereta' },
  { value: 'business', label: 'Business Loan', labelZh: '营运资金', labelMs: 'Pinjaman Perniagaan' },
];
```

**注意**: 这里已经有 `business`，但需要：
1. 将 `business` 改为 `sme_loan` 以匹配类型定义
2. 或者保持 `business` 但在辅助函数中映射到 `sme_loan`

**推荐方案**: 保持UI中使用 `business`，在辅助函数中映射

**修改辅助函数**（如果使用）:
```javascript
const loanTypeKey = loanType === 'personal' ? 'personal' :
                   loanType === 'housing' ? 'housing' :
                   loanType === 'auto' ? 'car' :
                   loanType === 'business' ? 'sme_loan' : // 映射 business 到 sme_loan
                   'personal';
```

#### Step 4: 更新辅助函数

在 `lib/bankStandardsReal2025.ts` 中的所有辅助函数需要支持 `sme_loan` 类型。

**检查函数**:
- `getDSRLimit()` - 需要支持 `sme_loan`
- `checkDSRRequirement()` - 需要支持 `sme_loan`
- `getRecommendedBanks()` - 默认参数需要支持 `sme_loan`

### SME Loan的特殊考虑

**企业贷款的特点**:
1. **DSR限制通常更宽松**: 65-85%（相比个人贷款60-75%）
2. **收入认定可能不同**: 基于公司营业额或利润
3. **需要额外文件**:
   - 公司注册证书（SSM）
   - 财务报表（P&L, Balance Sheet）
   - 银行对账单（12-24个月）
   - 商业计划书
4. **最低营业年限**: 通常需要2-3年
5. **担保要求**: 可能需要个人担保或公司资产抵押

**在数据结构中的体现**:
- DSR限制按收入区间设定（通常以公司月均营业额或利润计算）
- 收入认定规则可能需要单独的 `business` 就业类型规则
- 贷款限制可能需要单独的 `smeLoanMax` 字段

### 实施检查清单

- [ ] 更新 `LoanType` 类型定义（添加 `sme_loan`）
- [ ] 更新所有15家银行的 `dsr` 对象（添加 `sme_loan` 数组）
- [ ] 更新 `DSRCalculator` 组件的贷款类型选择（确保 `business` 映射到 `sme_loan`）
- [ ] 更新所有辅助函数支持 `sme_loan`
- [ ] 测试SME Loan的DSR计算
- [ ] 测试银行对比功能（包含SME Loan）
- [ ] 更新文档说明

---

## ⚠️ 重要说明

### 数据来源限制

1. **公开信息有限**: DSR限制、收入认定规则等详细指标通常不公开
2. **数据推断**: 新银行和SME Loan的数据基于：
   - 搜索到的利率信息
   - 现有8家银行的模式
   - 行业一般标准
   - 合理推断

### 建议

1. **验证数据**: 建议通过以下方式验证：
   - 直接联系银行获取最新政策
   - 参考实际申请案例
   - 咨询专业贷款顾问

2. **标记推断数据**: 在代码中标记哪些数据是推断的，哪些是确认的

3. **定期更新**: 银行政策可能变化，需要定期更新

---

## ✅ 实施建议与时间表

### 阶段1: 核心修复（立即实施 - 本周）

**优先级**: 🔴 最高

1. ✅ **修复月供计算公式**（30分钟）
2. ✅ **使用完整银行标准数据**（1小时）
3. ✅ **应用身份类型规则**（1.5小时）

**总工作量**: 约3小时

### 阶段2: 数据扩展（第1周）

**优先级**: 🔴 高

4. ✅ **添加SME Loan贷款类型**（1小时）
   - 更新类型定义
   - 更新所有现有银行数据
   - 更新组件和辅助函数

5. ✅ **整合7家新银行数据**（2小时）
   - 将新银行数据添加到 `bankStandardsReal2025.ts`
   - 包含SME Loan数据
   - 测试银行对比功能

**总工作量**: 约3小时

### 阶段3: 功能增强（第2周）

**优先级**: 🟡 中

6. ✅ **增强收入计算**（1小时）
7. ✅ **增强债务管理**（1小时）
8. ✅ **添加结果导出**（2小时）

**总工作量**: 约4小时

### 总计工作量

- **阶段1**: 3小时
- **阶段2**: 3小时
- **阶段3**: 4小时
- **总计**: 约10小时

---

## 📝 实施检查清单

### 核心修改
- [ ] 修复月供计算公式（使用减余法）
- [ ] 导入并使用完整银行标准数据
- [ ] 应用身份类型规则到计算中
- [ ] 移除硬编码的简化银行数据

### 数据扩展
- [ ] 添加 `sme_loan` 到 `LoanType` 类型定义
- [ ] 更新所有15家银行的 `dsr` 对象（添加 `sme_loan`）
- [ ] 添加7家新银行的完整数据
- [ ] 更新所有辅助函数支持新类型和新银行

### 功能增强
- [ ] 增强收入计算（津贴、奖金等）
- [ ] 增强债务管理（其他债务类型）
- [ ] 添加结果导出功能

### 测试验证
- [ ] 测试月供计算准确性
- [ ] 测试银行对比功能（15家银行）
- [ ] 测试SME Loan DSR计算
- [ ] 测试身份类型规则应用
- [ ] 测试所有贷款类型的计算

---

## 📚 参考文档

### 内部文档
- **原修改计划**: `DSR-CALCULATOR-DETAILED-MODIFICATION-PLAN.md`
- **功能分析**: `DSR-CALCULATOR-FEATURE-ANALYSIS.md`
- **银行标准数据**: `lib/bankStandardsReal2025.ts`
- **DSR计算库**: `lib/dsrCalculator.ts`

### 外部资源包（完整贷款审批标准）

**位置**: `/Users/1491-3913zee/Desktop/CALCULATOR DETAILS/`

1. **`Bank_Fintech_Approval_Criteria.md`** (330行)
   - 完整的贷款审批标准百科
   - 20家公司（6传统银行 + 5数字银行 + 5 BNPL + 4其他FinTech）
   - 20项核心指标定义 & 权重表
   - 9个详细章节
   - 4种权重模型对比
   - 批准决策树
   - 改善行动计划

2. **`Bank_Fintech_Approval_Matrix.csv`** (42行)
   - Excel可导入表格
   - 20家公司 × 18个关键指标列
   - 适合数据库和API集成
   - 包含：最低月收入、DSR上限、利率范围、批准速度、权重设置等

3. **`Quick_Reference_Guide.md`** (372行)
   - 快速参考和实战指南
   - 指标速查表（4类 × 20项）
   - 权重模型直观对比
   - 自评表（填空计算评分）
   - 30天改善清单
   - 常见错误与陷阱

4. **`Resource_Package_Summary.md`**
   - 使用指南和导航
   - 按用途分类的使用建议
   - 集成建议（如何加入 `/loan-optimizer`）
   - 常见问题FAQ

### 资源包核心数据概览

**覆盖的20家公司**:
- **传统银行（6家）**: Maybank, CIMB, RHB, AmBank, Hong Leong, Public Bank
- **数字银行（5家）**: GXBank, Boost Bank, AEON Bank, KAF Digital Bank, Ryt Bank
- **BNPL（5家）**: Grab PayLater, Shopee PayLater, Atome, Hoolah, myIOU
- **其他FinTech（4家）**: Grab Cash-i, GoPinjam, MOE Credit, Funding Societies

**关键指标数据**:
- 最低月收入: RM 1,500 - RM 3,500（传统银行）
- DSR容忍度: 60%（多数银行）到无限制（BNPL）
- CTOS最低分: 650（传统银行）到不必需（BNPL）
- 利率范围: 3% - 18%
- 批准速度: 最短24小时（CIMB）到2-4周（P2P）

**可用于的功能模块**:
1. 银行匹配模块（基于DSR + CTOS + 就业类型推荐最佳3家银行）
2. 改善建议模块（检测弱点，给出针对性建议）
3. 申请前检查模块（自动验证是否满足目标银行门槛）
4. 进度跟踪模块（跟踪CTOS变化，重新推荐银行）

---

**文档完成日期**: 2025-01-29
**资源包集成状态**: ✅ 已记录，待整合
**下一步**: 开始实施阶段1的核心修复，然后进行阶段2的数据扩展，并整合资源包数据

