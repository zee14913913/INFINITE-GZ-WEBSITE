# 服务器错误修复指南

## ✅ 构建状态
- **构建成功**: 所有24个页面正确生成
- **无编译错误**: TypeScript和ESLint检查通过
- **无类型错误**: 所有类型检查通过

## 🔧 如果遇到服务器错误，请尝试以下步骤：

### 1. 清理并重新构建
```bash
# 清理缓存
rm -rf .next
rm -rf node_modules/.cache

# 重新构建
npm run build

# 启动开发服务器
npm run dev
```

### 2. 检查端口占用
如果端口3000被占用，Next.js会自动使用下一个可用端口（3001, 3002等）
```bash
# 检查端口占用
lsof -i :3000

# 或者指定端口
PORT=3001 npm run dev
```

### 3. 检查Node版本
项目需要 Node.js 18+ 版本
```bash
node --version
# 应该显示 v18.x.x 或更高
```

### 4. 重新安装依赖
```bash
rm -rf node_modules
rm package-lock.json
npm install
npm run build
npm run dev
```

### 5. 检查环境变量
确保没有缺失的环境变量文件
```bash
# 检查是否有 .env 文件
ls -la | grep .env
```

### 6. 查看详细错误信息
如果错误仍然存在，查看完整的错误堆栈：
```bash
npm run dev 2>&1 | tee server-error.log
```

### 7. 常见错误及解决方案

#### 错误: "Cannot find module"
```bash
# 解决方案：重新安装依赖
rm -rf node_modules
npm install
```

#### 错误: "Port already in use"
```bash
# 解决方案：使用其他端口
PORT=3001 npm run dev
```

#### 错误: "Hydration error"
- 这通常是客户端/服务端组件不匹配导致的
- 确保所有使用浏览器API的组件都有 `'use client'` 指令

#### 错误: "localStorage is not defined"
- 确保使用 `localStorage` 的代码在 `useEffect` 中
- 确保组件有 `'use client'` 指令

## 📋 当前配置状态
- ✅ Next.js: 14.0.4
- ✅ React: 18.3.1
- ✅ TypeScript: 5.3.3
- ✅ Tailwind CSS: 3.4.0
- ✅ 所有页面构建成功

## 🚀 快速启动
```bash
# 1. 清理缓存
rm -rf .next

# 2. 构建项目
npm run build

# 3. 启动开发服务器
npm run dev
```

服务器应该会在 `http://localhost:3000` 启动（或显示的其他端口）。

